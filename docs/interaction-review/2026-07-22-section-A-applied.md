> ⚠ **SUPERSEDED (2026-07-22).** This doc was generated from the workflow agents' pre-reconciliation narration and does NOT match the committed JSONL or the current engine (the clinician review caught the drift). Use the authoritative, JSONL-derived packet instead: **[2026-07-22-section-A-reconciled.md](2026-07-22-section-A-reconciled.md)**. Kept only for history.

# Batch 1 v2 — Section A applied (12 warfarin/DOAC edits)

Generated 2026-07-22 from an adversarially-verified workflow (Edit -> Clinical-verify -> Schema-verify -> Reconcile, 48 agents). **Draft proposals for clinician sign-off — NOT promoted.** All citations remain `<verify>`. Author + independent approver still required (Task 7).

## Engine changes made to support these rules (TDD, all tests green)
1. **Tie-break precedence:** a *present, matched* context modifier now surfaces its impairment-specific management even when its severity+action tie the base (previously the base won the tie and the override text was lost — making text-only modifiers silently inert). Fixes warfarin+NSAID hepatic guidance and dabigatran renal-band text without distorting base severity.
2. **N-ary `all_of_present` combination matching:** the engine can now evaluate a rule requiring N>2 distinct agents (aspirin AND a P2Y12 inhibitor AND an oral anticoagulant). The pairwise-only engine previously made the triple-therapy rule silently never fire. Coverage-gap reporting now recurses into combination members. Added member sets `p2y12_inhibitor`, `oral_anticoagulant`.
3. **Mechanical fix:** the reconcile pass had dropped warfarin__nsaid_systemic`s `evidence` array; restored from the original (still `<verify>`).

## Per-rule summary

### warfarin__nsaid_systemic
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - dispense_action changed supply_with_counselling -> withhold_and_clarify: the backlog A-row edit requires prescriber/anticoagulation-plan confirmation for a NEW or unexplained warfarin+systemic-NSAID combination; withhold_and_clarify is the machine default because the engine cannot see therapy history, so the safety-forward posture is to confirm the plan (blocker #3, backlog A row 1).
  - Continuation bypass encoded in prescriber_action, not the dispense_action: an established, monitored, intentional combination with a documented INR plan should continue-and-monitor with no unilateral interruption of specialist-directed therapy. A single dispense_action cannot capture both new-vs-established branches, so the nuance lives in prescriber_action per blocker #3.
  - prescriber_action now names the required INR monitoring plan + GI-bleed risk assessment for the new/unexplained case, keeps 'avoid systemic NSAIDs where possible / lowest dose, shortest course', and preserves the v1 rationale that gastroprotection is ASSESSED not assumed (no automatic gastroprotection).
  - context_modifiers reduced from two to empty: both were severity major == base with on_unknown:base and only text overrides. In the engine a present-factor candidate that ties the base severity/action loses the stable sort to the base candidate, so the override text never surfaces - i.e. they are the inert major->major/base modifiers that v2 blocker #5 says to drop. The general renal/hepatic caution is retained as prose in prescriber_action instead of a no-op predicate; no severity escalation is grounded in the instruction so none was invented.
  - severity kept Major and risk_basis kept additive_pd (v1: keep Major; blocker #2: keep additive PD distinct from explicit contraindication).
  - exceptions retains the PPI caveat and now states topical/ophthalmic NSAIDs are out of scope (systemic route only), matching 'do not expand NSAID to topical/ophthalmic formulations'; object/perpetrator route already enforce systemic-only.
  - All <verify> citation placeholders, revision_date and accessed_at preserved unchanged; no new citation, dose cap, percentage, or named carve-out invented.
- **fabrications stripped -> `<verify>`:**
  - mechanism: the flatly-asserted clause 'some NSAIDs may also raise INR' was stripped and replaced with a <verify: ...> placeholder describing the exact claim (possible CYP2C9 interaction / protein-binding displacement) a clinician must confirm against the warfarin label. No replacement number or named exception was invented.
- **YOUR decisions (open clinical/citation questions):**
  - Mechanism: confirm and cite (or drop) whether some NSAIDs raise INR via CYP2C9 interaction / protein-binding displacement. Traceable to the <verify: ...> marker in the mechanism field.
  - Monitoring: supply a label/guideline citation for checking INR after both starting AND stopping the NSAID. Traceable to the <verify: ...> marker in management.monitoring.
  - Renal impairment: decide whether to add a renal context_modifier and, if so, which grounded eGFR/CrCl threshold and units to use (none encoded because the instruction gives no numeric cutoff). Traceable to the <verify: ...> marker in applicability.renal.
  - Over-alert acceptance: dispense_action is fixed withhold_and_clarify and fires on EVERY dispense, including established, stable, intentionally-monitored patients. The required continuation bypass for established therapy exists only as prescriber_action prose (confirm_and_monitor) because the engine has no 'established-therapy' context factor - the reviewer must knowingly accept the bypass is advisory-only. Traceable to the continuation clause in management.prescriber_action.
  - Evidence/jurisdiction: a single US FDA anchor is present while jurisdiction asserts US/UK/IN; UK/IN anchors and the excerpt/dates remain uncited. Traceable to the <verify> placeholders in the evidence array (matches the documented promotion-stage anchor limitation).

### warfarin__miconazole_oromucosal_gel
- **verifier:** clinical=sound, ready_for_review=true
- **changes:**
  - Encoded the jurisdiction + OTC/Rx split (backlog A instruction) inside prescriber_action: UK OTC/pharmacy-pack = flagged as a contraindication <verify wording>, UK Rx (SmPC) = avoid / close INR-guided <verify>, IN and US status = <verify per jurisdiction>. Schema has no OTC/Rx field, so the supply-status split lives in prescriber_action as the granular rationale directs, with applicability.jurisdiction retained (UK/IN/US).
  - Added a second evidence item for the UK OTC/pharmacy pack ('Do not use' contraindication wording) alongside the existing Rx SmPC item, so the OTC-vs-Rx status split has distinct source anchors (blocker 1 + blocker 4). All excerpts/dates remain <verify>.
  - Added the no-unilateral-stop nuance (blocker 3): new/unexplained combo => withhold_and_clarify + refer; established prescriber-initiated INR-monitored therapy => do not stop, confirm plan and continue monitoring. Captured in prescriber_action since a single dispense_action cannot express both.
  - Kept perpetrator restricted to route [oromucosal, oral] + formulation [oral_gel, oromucosal_gel] and hardened mechanism/exceptions to explicitly exclude topical skin/nail miconazole (blocker 1: no mechanical expansion to low-absorption topicals).
  - Dropped the hepatic child_pugh_b context_modifier: engine testing shows it is inert — base is already major + withhold_and_clarify (top action rank), so on the severity/action tie the base candidate sorts first and the override text never surfaces even when Child-Pugh B is present. Per the engine contract ('do NOT add inert modifiers') set context_modifiers to []. Resolution is unchanged (major / withhold_and_clarify) with or without it, confirming inertness.
  - Kept severity base at major (not contraindicated): forcing base contraindicated would trip the engine invariant to withhold_and_clarify unconditionally, defeating the established-monitored-Rx confirm/continue pathway (blocker 3); the possible contraindicated status is jurisdiction/OTC-specific and is carried as flagged <verify> prose rather than asserted as a hard base tier (anti-fabrication).
- **YOUR decisions (open clinical/citation questions):**
  - Base severity tier: currently 'major'. UK OTC labelling reads as a contraindication (clinician note row 3/E). Reviewer may optionally elevate base severity to 'contraindicated' to mirror the UK-OTC worst case; the engine would force withhold_and_clarify either way (already the current action). Tied to the <verify: exact UK OTC pack contraindication wording> marker in prescriber_action / the OTC evidence excerpt.
  - US applicability: 'US' is retained in applicability.jurisdiction but no systemically-absorbed miconazole oral/oromucosal-gel product is confirmed as US-marketed. Reviewer must confirm such a product exists before the rule fires in US context, or remove 'US'. Tied to the <verify per jurisdiction, including whether a systemically-absorbed miconazole oral/oromucosal product is marketed> marker in prescriber_action.
  - Per-jurisdiction OTC-vs-Rx contraindication wording: exact UK OTC pack 'Do not use' wording, exact UK SmPC 4.5 wording, and IN status all remain <verify> and must be sourced. Tied to the three <verify> markers in prescriber_action and the two evidence excerpts.
  - Encoding choice (informational): the OTC/Rx and jurisdiction 'split' is carried via two MHRA evidence items plus prescriber_action prose rather than separate rules, because the schema has no supply-status applicability dimension and context_modifier factors are renal/hepatic only. Reviewer may accept this or request the pack split into per-jurisdiction rules.

### warfarin__fluoroquinolone
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - monitoring: rewrote from the vague 'INR during therapy and after stopping' into an explicit, machine-testable start-AND-stop plan — an early post-start INR check plus a post-completion check, with more frequent monitoring if the course is prolonged (satisfies the backlog edit 'encode an INR plan after start/stop' and blocker 6 machine-testable fields).
  - prescriber_action: made the INR arrangement explicit for both start and stop of the course and added dose-adjustment guidance, instead of the softer 'consider an INR check if prolonged'.
  - patient_counselling: expanded 'Report bleeding signs' into concrete standard bleeding-sign examples plus 'do not stop/change warfarin without advice' (no unilateral-stop implication, blocker 3).
  - timing: populated (was null) with the start/stop framing of the INR checks so the plan is captured in a discrete field.
  - mechanism: added an explicit note that this is the metabolic/anticoagulation-monitoring interaction and is distinct from the additive QT risk of some fluoroquinolones — per the clinician instruction to keep the curated fluoroquinolone class and NOT conflate it with the QT class.
  - Preserved: rule_id, risk_basis (pk_perpetrator), base severity (moderate — clinician said acceptable), conditional, object/perpetrator/applicability, dispense_action (supply_with_counselling), empty context_modifiers (no renal/hepatic branch in the instruction; avoided adding inert modifiers), all <verify> evidence placeholders, review block. Fluoroquinolone class already resolves to exactly ciprofloxacin/levofloxacin/moxifloxacin/ofloxacin/norfloxacin in the member sets, so no membership change was needed.
- **fabrications stripped -> `<verify>`:**
  - mechanism clause 'gut-flora disruption reducing vitamin K' — a contested mechanistic specific not present in the review instruction; replaced with generic 'reported gut-flora effects <verify: gut-flora / vitamin-K contribution — contested pathway, confirm before asserting>'.
  - evidence[0].normalized_proposition 'INR monitoring is advised on starting and stopping' — embedded the reviewer's start/stop monitoring cadence as if it were verified FDA ciprofloxacin label text; replaced with the label-supported general proposition and the cadence retained only in management.
  - jurisdiction ['US','UK','IN'] breadth asserted on a single US FDA anchor — the unsupported UK/IN breadth was backed with explicit pending <verify> anchors (uk-anchor-pending / in-anchor-pending) rather than a bare claim; no fabricated UK/IN label content was invented.
- **YOUR decisions (open clinical/citation questions):**
  - Confirm or drop the gut-flora / vitamin-K contribution to the mechanism (traceable to the <verify: gut-flora / vitamin-K contribution ...> marker in `mechanism`). It is contested textbook pharmacology, not stated in the review instruction.
  - Provide and verify UK (MHRA SmPC / BNF) and IN (CDSCO) label anchors to justify the jurisdiction ['US','UK','IN'] scope, or narrow the jurisdiction to what is anchored (traceable to the two pending evidence items uk-anchor-pending and in-anchor-pending, whose product/section/excerpt/proposition are all <verify>).
  - Standard citation verification of the FDA ciprofloxacin anchor (evidence[0].excerpt, revision_date, accessed_at all remain <verify> — not performed this pass).
  - Confirm applying the class effect uniformly to all 5 curated fluoroquinolones while flagging ciprofloxacin/levofloxacin as the usual reported agents is acceptable (per instruction to keep the class curated and not conflate with QT).

### warfarin__tramadol
- **verifier:** clinical=sound, ready_for_review=true
- **changes:**
  - risk_basis: pk_perpetrator -> additive_pd. The backlog asks for 'observed_clinical/multifactorial', which is NOT in the fixed enum (contraindication|pk_perpetrator|additive_pd). Chose additive_pd as the closest: it correctly removes the false 'clean single-enzyme PK perpetrator' implication of pk_perpetrator (the v1 rationale is explicit that this is not a tidy CYP story) while staying honest that this is not an absolute contraindication. Mismatch flagged: the true nature is an observed, multifactorial clinical bleeding signal; additive_pd is the least-wrong of the three permitted values, not a perfect fit.
  - mechanism: reworded to state it is an observed clinical interaction that is multifactorial and incompletely defined, explicitly 'not a clean single-enzyme PK perpetrator effect' — aligns the prose with the new risk_basis. No enzyme named (avoids fabrication); language mirrors the review instruction.
  - Encoded the INR plan after start AND stop as machine-testable fields: monitoring = 'Check INR after starting tramadol AND after stopping tramadol'; prescriber_action repeats the start/stop INR check plus urgent bleeding advice and 'prefer an alternative analgesic where feasible' (v1 row 10).
  - Added no-unilateral-stop nuance to prescriber_action ('Do not unilaterally stop established warfarin therapy; clarify the analgesic plan with the prescriber') per batch blocker 3. Kept dispense_action = confirm_and_monitor (this is a Major additive/observed interaction on established, monitored anticoagulation, not a new unexplained contraindication), so no withhold_and_clarify.
  - Severity kept at major (v1 row 10: 'consider Major not Moderate' — already Major, preserved).
  - Dropped the pre-existing hepatic child_pugh_b context_modifier. It was functionally inert: same severity (major) and same action as base, and with on_unknown:base the engine never surfaces its override text (equal severity+action -> base candidate wins; verified: resolveRule under Child-Pugh B returns basis 'base'). Anti-inert-modifier engine rule requires its removal, and there is no grounding in the instruction to make it escalate (fabricating a hepatic threshold would violate the anti-fabrication rule). Base management is already neutral, so no caveat leak results.
  - Preserved all top-level keys, the <verify> citation strings, jurisdiction list, object/perpetrator selectors, and the historical severity note in exceptions.
- **fabrications stripped -> `<verify>`:**
  - The unqualified magnitude assertion 'markedly increased INR and severe/fatal bleeding' presented as established fact in the mechanism/exceptions prose - replaced with a <verify:...> placeholder pending FDA/pharmacovigilance confirmation (no replacement number invented); normalized_proposition softened from 'markedly increase' to 'increase'.
  - The implicit assertion that risk_basis='additive_pd' reflects an established additive pharmacodynamic mechanism - stripped and replaced with an explicit disclaimer (tramadol has no intrinsic anticoagulant/antiplatelet PD action) plus a <verify:...> enum-confirmation marker; no alternative mechanism was invented.
- **YOUR decisions (open clinical/citation questions):**
  - risk_basis enum: confirm the closest forced-enum value for an observed/multifactorial interaction - additive_pd (chosen) vs pk_perpetrator - since neither is a clean fit and the instruction rejected a clean single-enzyme PK framing. Traceable to the <verify> markers in mechanism and management.exceptions.
  - Bleeding magnitude: confirm whether the reported 'markedly increased INR' and 'severe/fatal bleeding' magnitude (including the fatal-outcome wording, quoted from the v1 review instruction) is supported by the FDA tramadol label and/or pharmacovigilance data. Traceable to the <verify> markers in mechanism and the evidence excerpt.
  - Hepatic modifier: confirm dropping vs restoring the pre-edit hepatic child_pugh_b context_modifier that intensified INR monitoring in hepatic impairment (dropped here as out-of-scope for the risk_basis/INR-plan edit). Traceable to the <verify> marker in management.exceptions.
  - Jurisdiction anchors: applicability.jurisdiction asserts US/UK/IN but evidence carries only a US FDA anchor; add UK and IN label/source anchors at promotion. Evidence dates and excerpt remain <verify>.

### warfarin__azithromycin_oral
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - Kept base severity Moderate and risk_basis pk_perpetrator unchanged, per the authoritative backlog instruction (row 4 / warfarin__azithromycin_oral: 'Moderate + INR plan for new course').
  - Rewrote prescriber_action to remove any 'prefer azithromycin' framing: it now says to prefer an antibiotic WITHOUT the relevant interaction where clinically appropriate, explicitly notes azithromycin still alters anticoagulation control and carries QT considerations (so it is not an interaction-free default), and requires an INR plan when azithromycin is used — directly encoding the v1 granular rationale.
  - Strengthened monitoring to require an INR check after STARTING AND after STOPPING the course (the 'INR plan for a new course' the instruction mandates), matching the sibling warfarin__fluoroquinolone/tramadol start/stop pattern.
  - Expanded patient_counselling from bare 'Report bleeding signs' to concrete bleeding signs (bruising, prolonged bleeding, blood in urine/stool, melaena) — no fabricated specifics, same generic sign list used by warfarin__nsaid_systemic.
  - Reworded mechanism to keep the grounded 'lower PK interaction potential than clarithromycin/erythromycin' comparison while adding 'but not interaction-free', so the rule no longer reads as reassurance to prefer azithromycin.
  - Kept dispense_action = supply_with_counselling (consistent with the Moderate pk_perpetrator sibling warfarin__fluoroquinolone); a short new antibiotic course added to established warfarin with an INR plan is a counselling-plus-monitoring action, not withhold-and-clarify. Blocker 3 nuance (do not imply unilateral pharmacist stop of specialist therapy) is satisfied — no stop language.
  - Preserved every top-level key, the object/perpetrator/applicability selectors, the single evidence item with its literal <verify> excerpt/dates, empty context_modifiers, proposed_status, and review block unchanged.
- **fabrications stripped -> `<verify>`:**
  - '(lower risk than clarithromycin)' comparative removed from the FDA azithromycin evidence item's normalized_proposition — not entailed by that item's own excerpt (which concerns only monitoring PT/INR) and unlikely to appear on an azithromycin label; replaced with a proposition constrained to the excerpt.
  - 'weak/uncertain CYP effect and lower PK interaction potential than clarithromycin/erythromycin' removed as an asserted-as-fact claim in the mechanism string; replaced with a <verify: ...> placeholder describing exactly what a clinician must confirm, with no invented number, dose cap, or named carve-out substituted.
- **YOUR decisions (open clinical/citation questions):**
  - Confirm and cite the comparative pharmacology of azithromycin vs clarithromycin/erythromycin (relative CYP3A4 inhibition potency and lower PK interaction potential) before any comparative wording is asserted — marked by the <verify: ...> placeholder in the mechanism string. Directionally standard but unverified this pass; must NOT be re-phrased so azithromycin reads as an interaction-free/safe default.
  - At promotion, consider anchoring the warfarin-INR proposition to a warfarin-label evidence item in addition to the perpetrator (azithromycin) label; the current single evidence item is the FDA azithromycin label (non-blocking, schema verifier note).
  - Reviewer note (pre-existing, not introduced by this edit): perpetrator route is ['oral','systemic'] under a rule_id suffixed '_oral'; IV azithromycin interaction is equal or greater, so it is a scope-label mismatch rather than a safety gap — resolve the naming/scope at promotion.
  - supply_with_counselling relies on the prescriber executing the INR plan encoded in prescriber_action/monitoring; this is the correct division of responsibility, but the reviewer should confirm the INR-after-start-and-after-stop plan is acceptable as the managing control for a new course.

### apixaban__strong_cyp3a4_pgp_inhibitor
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - Perpetrator restricted to combined strong dual CYP3A4/P-gp inhibitors and named explicitly: kept class cyp3a4_pgp_inhibitor + strength:[strong] (curated class already models combined dual inhibitors) and added perpetrator.member_notes enumerating the strong members and the US-label-named examples (ketoconazole, itraconazole, ritonavir, clarithromycin) — satisfies blocker #1 'named explicitly' without inventing membership.
  - Encoded the dose-dependent branch structurally: management.prescriber_action + mechanism + management.exceptions now carry the US-label 50% reduction on 5/10 mg BID and avoid on 2.5 mg BID. The context vocabulary is renal/hepatic only (no dose predicate), so the dose branch is machine-readable structured management text, not a context_modifier — the correct place per the engine contract.
  - Clarithromycin carve-out encoded as a FLAG, not asserted as fact: clarithromycin remains a matching member (verified: apixaban+clarithromycin still fires the rule) with a <verify: clarithromycin exception per US label> flag in both member_notes and management.exceptions. Deliberately NOT placed in member_exceptions, which would silently suppress the alert and assert the carve-out as established fact (v1 rationale forbids that).
  - Kept dispense_action withhold_and_clarify (base) and added the established-therapy nuance to prescriber_action per blocker #3 — pharmacist clarifies the prescriber-directed dose change and does not unilaterally halve or stop apixaban; established monitored therapy => confirm the decision rather than interrupt supply.
  - Removed the two context_modifiers (renal crcl_lt_30, hepatic child_pugh_c). With base already severity=major + dispense_action=withhold_and_clarify, both are provably inert: on the present-path they tie base and their management_override text never surfaces; on the escalate-absent path they reproduce base severity+action. Engine contract forbids inert modifiers and the v2 cleanup dropped exactly these. Verified via resolveRule at crcl20/child_pugh_c. NOTE for reviewer: if a hepatic child_pugh_c escalation to a HIGHER tier (apixaban not recommended in severe hepatic impairment) is intended, that is a separate drug-condition restriction (blocker #5, evaluate independently) and needs its own grounded encoding, not an inert modifier here.
  - Added indication/jurisdiction specificity: applicability.indication now states the action is dose-band-driven; jurisdiction kept [US,UK,IN] but the 50%/avoid branch flagged US-label-specific with a <verify> for UK SmPC and Indian label actions (evidence anchor remains US-only per the batch convention).
  - Recorded the renal metric as CrCl (Cockcroft-Gault) implicitly by retaining crcl_* semantics — but since the modifiers were removed as inert, no renal predicate remains; the label's CrCl basis is noted in prose only.
  - Preserved all 14 top-level keys, risk_basis=pk_perpetrator, severity=major, and every <verify> citation placeholder unchanged.
- **fabrications stripped -> `<verify>`:**
  - Confident assertion (in mechanism and perpetrator.member_notes) that the US apixaban label 'names ketoconazole, itraconazole, ritonavir, clarithromycin as examples of combined strong dual inhibitors', stated against an unverified <verify> citation — replaced with a <verify: US apixaban label examples of combined strong dual CYP3A4/P-gp inhibitors> placeholder.
  - Runtime classification of voriconazole, cobicistat and posaconazole as combined strong DUAL CYP3A4+P-gp inhibitors (dual/P-gp character unverified; voriconazole is a strong CYP3A4 inhibitor but not an established P-gp inhibitor) — removed from matching via member_exceptions and replaced with a <verify> placeholder pending confirmation of dual-inhibitor status.
- **YOUR decisions (open clinical/citation questions):**
  - Whether clarithromycin is a named exception with a smaller apixaban exposure rise per the applicable US label — kept in the matching set conservatively, flagged at perpetrator.member_notes and management.exceptions as <verify: clarithromycin exception per US label>.
  - UK SmPC and Indian label actions for apixaban + combined strong dual CYP3A4/P-gp inhibitors — jurisdiction is currently [US] only; extending coverage requires verified non-US actions. Marked at management.exceptions as <verify: UK SmPC and Indian label actions ... add as a jurisdiction-flagged branch or separate rule before extending jurisdiction>.
  - Whether voriconazole, cobicistat and posaconazole qualify as combined strong DUAL CYP3A4+P-gp inhibitors and should be re-included in the matching set — currently excluded via member_exceptions. Marked at perpetrator.member_notes as <verify: confirm ... combined strong DUAL CYP3A4+P-gp inhibitors>.
  - Which specific drugs the US apixaban label names as combined strong dual-inhibitor examples — marked in mechanism and member_notes as <verify: US apixaban label examples of combined strong dual CYP3A4/P-gp inhibitors>.
  - All evidence excerpt/revision_date/accessed_at remain <verify> and require citation verification in a later pass (not performed here).

### rivaroxaban__strong_cyp3a4_pgp_inhibitor
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - see above
- **fabrications stripped -> `<verify>`:**
  - Prose claim in management.prescriber_action that voriconazole and posaconazole are 'combined strong CYP3A4 AND P-gp inhibitors' as established fact — voriconazole's P-gp inhibition is weak/unestablished and posaconazole's is modest; replaced with a <verify: per-member combined-dual-inhibitor confirmation> placeholder plus member_notes.
  - Prose claim in management.prescriber_action listing clarithromycin inside the avoid set as settled fact ('avoid concomitant use with a combined strong CYP3A4 AND P-gp inhibitor (clarithromycin, ...)') — its rivaroxaban-label position is unverified; replaced with a <verify> placeholder cross-referencing member_notes.clarithromycin (member kept covered, conservative direction).
- **YOUR decisions (open clinical/citation questions):**
  - Confirm the rivaroxaban-label position on clarithromycin specifically: is it inside the combined strong CYP3A4/P-gp avoid set, or does the label carve it out (as apixaban's does)? Do NOT assume the apixaban nuance applies. (Marker: perpetrator.member_notes.clarithromycin, management.exceptions, management.prescriber_action.)
  - Confirm per-member that voriconazole and posaconazole qualify as COMBINED strong CYP3A4 AND P-gp inhibitors for rivaroxaban before promotion — voriconazole's P-gp inhibition is weak/unestablished and posaconazole's is modest. (Marker: perpetrator.member_notes.voriconazole, perpetrator.member_notes.posaconazole, management.prescriber_action, management.exceptions.)
  - Confirm the renal severity-escalation threshold: crcl_lt_30 may under-scope if the label's combined-inhibitor renal caution extends above CrCl 30 (sibling moderate-inhibitor rule uses CrCl 15–<80). No safety hole (base action is already withhold_and_clarify) but the escalated severity tier may need a higher cutoff. (Marker: context_modifiers[renal].management_override.prescriber_action.)
  - Confirm on_unknown:'escalate' is the intended policy for the hepatic Child-Pugh B/C drug-condition restriction (labels every unknown-hepatic dispense 'contraindicated') versus on_unknown:'base' (reserves 'contraindicated' for confirmed Child-Pugh B/C). Engine-legal and safe either way; over-alert direction only. (Marker: context_modifiers[hepatic] child_pugh_b/child_pugh_c.)
  - Verify all citation excerpts/revision_date/accessed_at (left as <verify> — not this pass).

### edoxaban__pgp_inducer
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - Perpetrator changed from `class: pgp_inducer` (strength strong; members rifampicin/carbamazepine/phenytoin/St John's wort) to the exact named drug `rifampicin` — implements the backlog instruction 'only named inducers per jurisdiction' and blocker 1 (no mechanical strong/P-gp-inducer class expansion). Verified in the engine: edoxaban+rifampicin now fires; edoxaban+carbamazepine no longer fires.
  - Kept perpetrator route [oral, systemic] and added formulation [] per the class-ref modifier requirements (blocker 1: preserve route/formulation).
  - dispense_action changed withhold_and_clarify -> confirm_and_monitor (blocker 3): edoxaban is established specialist-directed anticoagulation and this is a pk_perpetrator (efficacy-loss) interaction, not a contraindication, so the pharmacist must not imply a unilateral stop. The new-vs-established nuance a single action can't capture is encoded in prescriber_action (clarify if rifampicin newly added/unexplained; confirm plan if established).
  - mechanism tightened to attribute induction specifically to rifampicin and to state the P-gp (not combined CYP3A4) mechanism distinguishing it from apixaban/rivaroxaban (v1 row-14 rationale).
  - management fields made structured/executable (blocker 6): concrete monitoring (thrombotic events), counselling (adherence + clot symptoms), duration kept neutral (no fabricated offset window), and exceptions now documents the named-inducer scoping and explicitly warns against mechanically adding carbamazepine/phenytoin/SJW without per-jurisdiction label grounding (<verify>).
  - No context_modifiers added — none are grounded in the instruction and adding a renal/hepatic branch here would be inert or fabricated (engine anti-inert rule).
  - Preserved all <verify> citation placeholders, evidence metadata skeleton, severity (major), risk_basis (pk_perpetrator), conditional (true), applicability, proposed_status, review, and _section unchanged.
- **fabrications stripped -> `<verify>`:**
  - 'per label coadministration is generally not recommended' (management.prescriber_action) — unverified label wording lifted from the instruction's illustrative 'e.g.' example and mis-attributed to the FDA edoxaban label; replaced with a jurisdiction-split <verify> placeholder.
  - 'coadministration generally not recommended' (evidence.normalized_proposition, attributed to source_id fda-label-edoxaban) — same unverified/mismatched label verb; replaced with a neutral grounded proposition (exposure reduction + efficacy-loss) with the exact coadministration handling left as <verify>.
- **YOUR decisions (open clinical/citation questions):**
  - Confirm the exact edoxaban-label handling of rifampicin coadministration for EACH jurisdiction (US FDA/Savaysa, UK/EU SmPC, IN) — 'avoid concomitant use' vs 'generally not recommended' vs other. Marker: <verify> in management.prescriber_action, evidence.excerpt, and evidence.normalized_proposition.
  - Decide whether carbamazepine, phenytoin, and St John's wort are label-named potent inducers of edoxaban in US/UK/IN; if so, add them as named-drug rules or a curated edoxaban_labelled_potent_inducer member set before promotion. Marker: <verify> in management.exceptions (hard promotion gate).
  - Sign off on the base dispense_action. It is set to withhold_and_clarify per the blocker-3 default for a new/unexplained combo (matching the pre-edit value); if a reviewer prefers confirm_and_monitor on the grounds that withholding the anticoagulant is itself harmful in an efficacy-loss interaction, that downgrade needs explicit clinician sign-off and the 'withhold before continuing' prose would need to be softened to match.

### dabigatran__pgp_inhibitor
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - Split by exact inhibitor: replaced the bare pgp_inhibitor class with an explicit named-member list on the perpetrator (dronedarone, ketoconazole, verapamil, amiodarone, quinidine, clarithromycin), keeping class+strength+route for engine matchability. Named dronedarone + systemic ketoconazole as the specific NVAF dose-reduction inhibitors in the modifier prose (per the backlog instruction).
  - Split by indication: set applicability.indication = [non_valvular_atrial_fibrillation, vte_treatment, vte_prophylaxis] and threaded the NVAF-vs-VTE branch difference through mechanism, base prescriber_action, and both context_modifier overrides — the VTE avoidance CrCl threshold is explicitly flagged as higher than NVAF (<verify).
  - Split by CrCl using the correct metric: switched to CrCl (Cockcroft-Gault) predicates only (crcl_lt_30, crcl_lt_60) — no egfr_* — and recorded the required metric+units in applicability.renal and the mechanism (blocker #5). Added a second modifier (crcl_lt_60, major, dose-adjust/clarify) below the existing crcl_lt_30 avoid branch to give a deterministic renal ladder.
  - Escalate-on-absence normalized (engine contract): crcl_lt_60 carries top-level severity=major AND dispense_action=withhold_and_clarify so unknown renal escalates action (confirm→withhold) with neutral base management; crcl_lt_30 set to on_unknown=base so mere absence of a lab does not force a 'contraindicated' verdict (avoids over-alerting; mirrors the apixaban/rivaroxaban siblings).
  - Neutralized base management so nothing renal/impairment-specific leaks on the escalate-on-absence path: removed 'use recent renal function to guide' and 'when renal function is adequate'; all renal/dose prose now lives only in the modifiers' management_override (blocker #5 / v2 base-leak fix).
  - Blocker #3 nuance encoded: base dispense_action stays confirm_and_monitor with 'do not independently stop established specialist-directed therapy', while unknown/reduced/severe renal escalate to withhold_and_clarify — capturing both the established-monitored and new-unclear-combo cases.
  - Refined mechanism/evidence to be inhibitor-, indication- and CrCl-specific; kept the single FDA evidence item with all citation fields as literal <verify>.
- **fabrications stripped -> `<verify>`:**
  - Reduced NVAF dabigatran dose '75 mg twice daily' — stripped; replaced with <verify: exact reduced dabigatran dose> in crcl_lt_60 management_override and evidence.excerpt.
  - CrCl values '≈30 mL/min', '≈30-50 mL/min', '≈50 mL/min' — stripped; the specific numbers are gone and the predicates are declared conservative <verify> proxies.
  - Assertion that 'verapamil, amiodarone, quinidine and clarithromycin differ' from dronedarone/ketoconazole — removed; those inhibitors are now excluded via member_exceptions with no behavioural claim, deferred to sibling rules.
  - 'systemic ketoconazole' asserted as a factual formulation/route restriction — softened to <verify: confirm ketoconazole formulation scope>; no systemic-only carve-out is asserted.
  - Generalisation that the VTE avoidance threshold '≈50 mL/min applies to all P-gp inhibitors' — removed; VTE scoped out to sibling rules and no threshold is asserted.
- **YOUR decisions (open clinical/citation questions):**
  - Exact CrCl avoidance threshold for NVAF: crcl_lt_30 is a conservative proxy — confirm the true threshold and whether it should be crcl_lt_15/30/60 (traces to crcl_lt_30 management_override <verify> and evidence.excerpt <verify>).
  - Exact CrCl dose-adjustment band and reduced dabigatran dose for NVAF: crcl_lt_60 is a conservative proxy — confirm band and reduced dose (traces to crcl_lt_60 management_override <verify> and evidence.excerpt <verify>).
  - Whether the ketoconazole caution applies only to systemic formulations (traces to mechanism <verify: confirm ketoconazole formulation scope>).
  - Per-inhibitor coverage: verapamil, amiodarone, quinidine, clarithromycin, itraconazole, ritonavir and ciclosporin are excluded via member_exceptions and currently match NO rule for dabigatran — sibling rules with their own management must be authored; exclusion is not a safety claim (traces to management.exceptions <verify> and perpetrator.member_exceptions).
  - VTE-treatment and VTE-prophylaxis need separate sibling rules with their own (per instruction, different) CrCl thresholds; this rule is NVAF-only (traces to management.exceptions <verify> and applicability.indication).
  - Jurisdiction scoped to US; UK and IN dabigatran labels (named inhibitors and thresholds may differ) must be checked before extending applicability.jurisdiction (traces to applicability.renal <verify>).
  - Base severity tier at confirmed-normal renal function (moderate) vs major — confirm the floor is appropriate.
  - on_unknown behaviour: crcl_lt_60 escalates unknown CrCl to major/confirm — confirm this is preferred over leaving unknown at base moderate.
  - The instruction lists dronedarone/ketoconazole as examples ('e.g.'); confirm the full set of named P-gp inhibitors requiring NVAF dose adjustment from the label (traces to evidence.normalized_proposition <verify>).

### clopidogrel__cyp2c19_inhibiting_ppi
- **verifier:** clinical=sound, ready_for_review=true
- **changes:**
  - Grounded the alternative-PPI advice: pantoprazole kept as the named lower-CYP2C19-inhibition example (grounded in the v1 rationale), rabeprazole moved from a confident named alternative to a '<verify>' placeholder in both prescriber_action and exceptions (anti-fabrication).
  - Confirmed and preserved the perpetrator restriction to omeprazole/esomeprazole (member set cyp2c19_inhibiting_ppi already = {omeprazole, esomeprazole}); no class expansion.
  - Kept the cimetidine caveat (itself a CYP inhibitor, not a generic-safe H2 alternative) and confirmed there is no 'or an H2 blocker' advice, per v1 review row 16.
  - Left dispense_action=confirm_and_monitor and 'do not stop clopidogrel' (blocker 3: no unilateral stop of established therapy) and risk_basis=pk_perpetrator (blocker 2). No context_modifiers/inert modifiers added; all <verify> citations preserved; all 14 keys retained.
- **YOUR decisions (open clinical/citation questions):**
  - Whether to include rabeprazole as a lower-CYP2C19-inhibition acid-suppressant alternative. The review instruction names only pantoprazole; adding rabeprazole (even behind a placeholder) slightly widens scope. Carried in prescriber_action as '<verify: rabeprazole may also have lower CYP2C19 inhibition>' — a reviewing clinician should either confirm rabeprazole's lower-inhibition status against a label/source or drop it and leave pantoprazole as the sole named alternative.
  - Standard citation verification (this pass does not verify): the FDA clopidogrel-label evidence item retains '<verify>' for revision_date, accessed_at, and excerpt and must be sourced before promotion.

### aspirin_ld_ir__ibuprofen_timing
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - object selector: added executable dose (`dose: low_dose_cardioprotective`) and `dose_band` (<verify> mg threshold, ungrounded), kept `formulation: [immediate_release]`, and added `formulation_exclusions: [enteric_coated, delayed_release]` so the matcher can encode 'low-dose IR aspirin' (backlog A row for this rule + batch blocker #4).
  - perpetrator selector: added `dosing_pattern: single_or_occasional` so the repeat-dose caveat becomes executable rather than prose-only; chronic/repeated ibuprofen now explicitly falls OUT of this rule.
  - management.timing converted from a free-text string carrying concrete '30 min / 8 h' numbers into a machine-testable object with the two spacing intervals as <verify> placeholders (blocker #6). The prior 30-min/8-h figures were not grounded in the review instruction, so per the anti-fabrication rule they were replaced, not asserted.
  - patient_counselling reworded to drop the fabricated 30-min/8-h numbers (now <verify>) and to state that repeated ibuprofen dosing undoes the spacing.
  - prescriber_action strengthened to say spacing does NOT reliably help with repeated/chronic ibuprofen (encodes the v1 'repeated ibuprofen defeats spacing' rationale).
  - exceptions clarified: EC/delayed-release aspirin, repeated/chronic ibuprofen, and other NSAIDs are explicitly excluded and pointed to the SEPARATE additive-GI-bleeding rule (kept un-merged per v1 review row 17).
  - mechanism reworded to state the interaction is a pharmacodynamic timing effect limited to IR low-dose aspirin + single/occasional ibuprofen (no new numeric claims).
  - Preserved unchanged: rule_id, risk_basis (additive_pd), severity (moderate), conditional, dispense_action (space_doses), context_modifiers ([]), all <verify> evidence citations, review block, _section. risk_basis left as-is because the selector edit does not authorize a basis change and blocker #2 only requires separation from contraindication, which holds.
- **fabrications stripped -> `<verify>`:**
  - object.formulation_exclusions 'delayed_release' — not grounded in the instruction (which named only enteric-coated). Replaced with a <verify: ...> placeholder asking the clinician to confirm whether delayed-release forms beyond enteric-coated should be excluded.
- **YOUR decisions (open clinical/citation questions):**
  - Exact spacing intervals: the minimum interval IR low-dose aspirin must precede ibuprofen, and the minimum interval ibuprofen must precede the aspirin dose — both are <verify> in management.timing.* and in patient_counselling. Clinician must ground these against a label/advisory.
  - Low-dose cardioprotective aspirin mg/day upper threshold — <verify> in object.dose_band; no mg number was invented.
  - Whether to exclude delayed_release aspirin formulations beyond enteric_coated — <verify> in object.formulation_exclusions (enteric-coated is itself delayed-release; instruction named only enteric-coated).
  - ENGINE decision: checkPair collision-suppression must be made risk_basis/mechanism-aware (or this rule and aspirin__nsaid_additive_gi_bleeding tagged mutually non-suppressing) so both the antiplatelet-timing and GI-bleeding findings co-surface for aspirin+ibuprofen — flagged in co_surface.engine_support. Recommend a checkInteractions(['aspirin','ibuprofen']) regression fixture asserting BOTH findings survive.
  - ENGINE decision: satisfies()/ruleMatchesPair must be extended to gate on formulation/dose/dosing_pattern for the v2 'make selector executable' instruction to be met; until then the narrowing fields are inert metadata — flagged in _selector_executability. Confirm that once gating is live, enteric-coated-aspirin and chronic-ibuprofen cases fall out of this timing rule but still land on the GI-bleeding rule.
  - Evidence citation for the FDA advisory (excerpt, revision_date, accessed_at) all remain <verify> — citation verification is a separate pass.

### dual_antiplatelet__oral_anticoagulant_triple_therapy
- **verifier:** clinical=needs_fix, ready_for_review=true
- **changes:**
  - SELECTOR now requires ALL THREE (backlog edit + v1 #18): object.combination changed from [aspirin, clopidogrel] to [aspirin, {class:p2y12_inhibitor}] so a P2Y12 inhibitor (clopidogrel/ticagrelor/prasugrel), not just clopidogrel, satisfies the antiplatelet slot; perpetrator remains the third required drug. Both antiplatelets in the combination AND the perpetrator OAC must be present for the rule to fire.
  - Perpetrator class narrowed from the undefined, jurisdiction-neutral 'anticoagulant' to 'oral_anticoagulant' (instruction says oral anticoagulant); route tightened from ['systemic','oral'] to ['oral'] on perpetrator and applicability. Both classes (p2y12_inhibitor, oral_anticoagulant) are NEW — named in new_classes_or_members_introduced; until members are added at promotion the engine will report them as honest coverage gaps.
  - dispense_action kept at confirm_and_monitor and severity kept at major (not contraindicated) per blocker 3 / v1 #18: never withhold established specialist-directed therapy. The withhold-if-unexplained nuance is carried entirely in prescriber_action, which now states the alert fires only on all-three-present, to confirm indication + planned duration, not to independently stop any antithrombotic, and to urgently escalate an unplanned/unexplained overlap — replacing the old 'avoid inadvertent overlap' framing.
  - risk_basis kept as additive_pd (blocker 2: additive PD bleeding, not an absolute contraindication). mechanism reworded to name all three agents and to state triple therapy is frequently intentional/time-limited (post-PCI in AF).
  - evidence citations preserved verbatim as <verify> (revision_date, accessed_at, excerpt); only normalized_proposition (my own structuring, not a citation) reworded to reflect all three drugs.
  - context_modifiers preserved (both on_unknown:base, non-escalating, text-only). Base management kept neutral (no renal/hepatic caveat prose); impairment prose lives only in management_override. Added a CrCl/Cockcroft-Gault note to the renal override per blocker 5 (DOAC renal dosing uses CrCl, not eGFR) without inventing a threshold.
- **fabrications stripped -> `<verify>`:**
  - Removed the 'antiplatelet (low) dose aspirin' / 'confirm dose band' qualifier from the aspirin selector member. 'Low dose' was an unrequested specific (the instruction said only 'aspirin') that risked hardening into a dose filter and excluding high-dose-aspirin triple therapy; replaced with an explicit no-dose-constraint note plus a <verify: confirm aspirin dose scope is intentionally unrestricted> placeholder. No dose number was ever invented, and none was added.
- **YOUR decisions (open clinical/citation questions):**
  - BLOCKING (engine capability, not fixable in the rule): the current pairwise engine cannot evaluate a three-agent 'all present among the subject set' selector - checkInteractions on ['aspirin','clopidogrel','warfarin'] verifiably returns 0 findings. Either the engine must gain n-ary all-of-these-present matching (marker: object.match_semantics='all_of_present' + object.description), or product must choose an alternate representable encoding. This must be resolved before promotion. Not a fabrication - an engineering/Task-7 decision.
  - Coverage-gap visibility (engine): the coverage-gap reporter only scans top-level object/perpetrator .class. oral_anticoagulant is now top-level (visible), but the nested p2y12_inhibitor combination-member class is invisible, so the honest-coverage net cannot flag it. Requires the engine's coverage scan to recurse into combination members (marker: object.combination p2y12_inhibitor note).
  - Class roster verification at promotion: add p2y12_inhibitor {clopidogrel, ticagrelor, prasugrel; ticlopidine/cangrelor?} and oral_anticoagulant {warfarin, acenocoumarol, phenindione, dabigatran, rivaroxaban, apixaban, edoxaban} to data-static/interaction-member-sets.json with IN/US/UK formulary check (markers: members lists + <verify: jurisdictional roster>).
  - Exact DOAC renal thresholds (per-agent CrCl mL/min cut-offs) left as <verify: exact per-agent thresholds> in the renal modifier and OAC member note - clinician/label confirm.
  - Member-specific hepatic restriction (e.g. rivaroxaban Child-Pugh B/C with coagulopathy) to be evaluated independently and combined - <verify> markers on the OAC member note and hepatic modifier; confirm exact per-member label wording.
  - on_unknown policy: renal and hepatic modifiers currently use on_unknown 'base' (no escalation on unknown organ function). The schema verifier notes v2 default policy may escalate for high-bleeding-risk combinations; clinician to confirm whether unknown renal/hepatic status should escalate (markers: on_unknown fields).
  - Aspirin dose scope intentionally left unrestricted (any dose captured); confirm this is the desired scope vs a low-dose-only rule (marker: aspirin member note <verify>).
  - All citations remain <verify> - a separate citation-verification pass is still required.
