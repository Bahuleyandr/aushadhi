# 2026-08-06 promoted-evidence drift captures

These files retain the exact public payloads used to reproduce the technical
promotion holds for `warfarin__azithromycin_oral` and `warfarin__tramadol`.
`capture-manifest.json` records the source URLs, capture time, byte hashes,
canonical payload hashes, and the independently observed DailyMed SPL identity.

- The openFDA record is published under CC0 1.0.
- The GOV.UK material contains public sector information licensed under the
  Open Government Licence v3.0.
- The DailyMed XML was used only as an independent identity reference. Its
  response hash and extracted set/version/effective-time metadata are recorded;
  its payload is not retained here.

These captures have no clinical-promotion or deployment authority. They show
why the historical payload bindings no longer match; they do not authorize the
changed sources or replace clinician review.
